import 'package:flutter/material.dart';

class CharacterDetailsScreen extends StatelessWidget {
  const CharacterDetailsScreen({super.key, required int characterId});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Character Details Screen'),
    );
  }
}